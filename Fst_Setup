# Move to main project directory
cd <main directory>

# Create Fst directory and its daughter directories
mkdir Fst
for i in $(cat specieslist); do mkdir Fst/${i}; done 

# Make copies of ConcatList.txt and helper6.txt from one of the other directories containing them to the Fst directory 
cp <location of files> Fst



